import { MoonLoader } from "react-spinners";

const Spinner = () => (
  <div className="mt-0 flex justify-center items-center min-h-screen">
    <MoonLoader size={300} color="#36d7b7" />
  </div>
);

export default Spinner;
